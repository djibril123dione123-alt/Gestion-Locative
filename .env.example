# =============================================================================
# Variables d'environnement - Samay Këur (Confort Immo Archi)
# =============================================================================
# Sur Replit, configurer ces valeurs via le panneau "Secrets" (icône verrou),
# JAMAIS dans un fichier commité au dépôt.
# =============================================================================

# URL publique de l'instance Supabase
# Format : https://<project-ref>.supabase.co
VITE_SUPABASE_URL=https://<your-project-ref>.supabase.co

# Clé anonyme (publique) Supabase
# Trouvable dans Project Settings > API > anon public
VITE_SUPABASE_ANON_KEY=<your-supabase-anon-key>

# Clé service (secrète) Supabase - pour les opérations serveur
# ATTENTION: Ne jamais exposer côté client !
# SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# Environnement (development/staging/production)
VITE_ENV=development

# URLs publiques de production.
# VITE_APP_URL / VITE_PUBLIC_APP_URL servent aux liens vers l'application.
# VITE_PUBLIC_VERIFY_BASE_URL est la seule base autorisée pour les QR documentaires imprimés.
# Ne jamais utiliser localhost ici, sinon les QR codes imprimés seront cassés.
VITE_APP_URL=https://app.samaykeur.com
VITE_PUBLIC_APP_URL=https://app.samaykeur.com
VITE_MARKETING_URL=https://samaykeur.com
VITE_PUBLIC_VERIFY_BASE_URL=https://samaykeur.com

# Configuration Sentry (optionnel - monitoring erreurs)
# VITE_SENTRY_DSN=
# Session Replay reste désactivé par défaut. N'activer qu'après validation confidentialité.
# VITE_SENTRY_REPLAY_ENABLED=false
# SENTRY_ORG=your-org-slug
# SENTRY_PROJECT=your-project-slug
# SENTRY_AUTH_TOKEN=your-auth-token

# Analytics PostHog (optionnel)
# VITE_POSTHOG_KEY=
# VITE_POSTHOG_HOST=https://eu.posthog.com
# VITE_POSTHOG_SESSION_RECORDING_ENABLED=false
